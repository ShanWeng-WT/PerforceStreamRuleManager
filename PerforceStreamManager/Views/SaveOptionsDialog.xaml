<Window x:Class="PerforceStreamManager.Views.SaveOptionsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Save Options" Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header Message -->
        <TextBlock Grid.Row="0" 
                   Text="Save stream rules and snapshot" 
                   FontWeight="SemiBold" 
                   FontSize="14"
                   Margin="0,0,0,15"/>
        
        <!-- Submit Option -->
        <CheckBox Grid.Row="1" 
                  Name="SubmitCheckBox"
                  Content="Submit snapshot file immediately"
                  IsChecked="True"
                  Margin="0,0,0,10"/>
        
        <TextBlock Grid.Row="2" 
                   Text="If unchecked, the snapshot file will be left in a pending changelist."
                   TextWrapping="Wrap"
                   Foreground="Gray"
                   FontSize="11"/>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button Content="Save" 
                    Width="90" 
                    Padding="10,5"
                    Click="SaveButton_Click"
                    IsDefault="True"/>
            <Button Content="Cancel" 
                    Width="90" 
                    Margin="10,0,0,0"
                    Padding="10,5"
                    IsCancel="True"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
