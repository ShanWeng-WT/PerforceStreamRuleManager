<Window x:Class="PerforceStreamManager.Views.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Settings" Height="450" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,0,10,0">
<!-- Perforce Connection Settings -->
                <GroupBox Header="Perforce Connection" Margin="0,0,0,10">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <Label Grid.Row="0" Grid.Column="0" Content="Server:"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Name="ServerTextBox" Margin="0,0,0,5"/>
                        
                        <Label Grid.Row="1" Grid.Column="0" Content="Port:"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Name="PortTextBox" Margin="0,0,0,5"/>
                        
                        <Label Grid.Row="2" Grid.Column="0" Content="User:"/>
                        <TextBox Grid.Row="2" Grid.Column="1" Name="UserTextBox" Margin="0,0,0,5"/>
                        
                        <Label Grid.Row="3" Grid.Column="0" Content="Password:"/>
                        <PasswordBox Grid.Row="3" Grid.Column="1" Name="PasswordBox" Margin="0,0,0,5"/>
                        
                        <Label Grid.Row="4" Grid.Column="0" Content="Status:"/>
                        <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <Ellipse Name="ConnectionStatusIndicator" Width="12" Height="12" 
                                     Fill="Red" Margin="0,0,5,0"/>
                            <TextBlock Name="ConnectionStatusText" Text="Not Connected"/>
                            <Button Name="TestConnectionButton" Content="Test Connection" 
                                    Margin="10,0,0,0" Padding="8,2" Click="TestConnectionButton_Click"/>
                        </StackPanel>
                    </Grid>
                </GroupBox>
                
                <!-- History Storage Settings -->
                <GroupBox Header="History Storage" Margin="0,0,0,10">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
<Label Grid.Row="0" Grid.Column="0" Content="Depot Path:"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Name="HistoryPathTextBox" IsReadOnly="True"/>
                    </Grid>
                </GroupBox>
                
                <!-- Retention Policy Settings -->
                <GroupBox Header="Retention Policy" Margin="0,0,0,10">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <Label Grid.Row="0" Grid.Column="0" Content="Max Snapshots:"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Name="MaxSnapshotsTextBox" Margin="0,0,0,5"/>
                        
                        <Label Grid.Row="1" Grid.Column="0" Content="Max Age (days):"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Name="MaxAgeDaysTextBox"/>
                    </Grid>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="Save" 
                    Width="80" 
                    IsDefault="True"
                    Click="SaveButton_Click"/>
            <Button Content="Cancel" 
                    Width="80" 
                    Margin="10,0,0,0"
                    IsCancel="True"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
