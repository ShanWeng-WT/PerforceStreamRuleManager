<Window x:Class="PerforceStreamManager.Views.DepotBrowserDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Browse Depot" Height="450" Width="500"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Selected Path Display -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <Label Content="Selected Path:"/>
            <TextBox Name="SelectedPathTextBox" 
                     IsReadOnly="True" 
                     Background="#F0F0F0"/>
        </StackPanel>
        
        <!-- Depot TreeView -->
        <GroupBox Grid.Row="1" Header="Depot Structure" Margin="0,0,0,10">
            <TreeView Name="DepotTreeView" 
                      SelectedItemChanged="DepotTreeView_SelectedItemChanged"
                      TreeViewItem.Expanded="DepotTreeView_Expanded">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                        <TextBlock Text="{Binding Name}"/>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </GroupBox>
        
        <!-- Status Bar -->
        <TextBlock Grid.Row="2" 
                   Name="StatusTextBlock" 
                   Text="Loading depot structure..." 
                   Margin="0,0,0,10"
                   Foreground="Gray"/>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="OK" 
                    Width="80" 
                    IsDefault="True"
                    Click="OkButton_Click"/>
            <Button Content="Cancel" 
                    Width="80" 
                    Margin="10,0,0,0"
                    IsCancel="True"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
